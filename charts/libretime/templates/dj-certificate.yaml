apiVersion: cert-manager.io/v1 
kind: Certificate
metadata:
  name: {{ .Values.dj.host }}-cert
  namespace: radio
spec:
  dnsNames:
  - {{ .Values.dj.host }}
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: {{ .Values.dj.host }}-cert
