apiVersion: batch/v1
kind: CronJob
metadata:
  name: library-scan
spec:
  schedule:  {{ .Values.libraryscan.schedule | squote }}
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: library-scan
              image: "{{ .Values.api.repository.image }}:{{ .Values.api.repository.tag }}"
              command: ['sh', '-c', 'flock -n /library/library-scan.lock libretime-api bulk_import --path /library']
              resources:
                requests:
                  cpu: 500m
                  memory: 1Gi
              volumeMounts:
                - mountPath: /etc/libretime/config.yml
                  subPath: config.yml
                  name: libretime-conf
                  readOnly: true
                - mountPath: /srv/libretime
                  name: libretime
                - mountPath: /library
                  name: library
          volumes:
            - name: libretime-conf
              secret:
                secretName: libretime-conf
                items:
                  - key: config.yml
                    path: config.yml
{{ .Values.volumes.libretime | indent 12 }}
{{ .Values.volumes.library | indent 12 }}