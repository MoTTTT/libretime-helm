apiVersion: cert-manager.io/v1 
kind: Certificate
metadata:
  name: {{ .Values.masterHost }}-cert
  namespace: radio
spec:
  dnsNames:
  - {{ .Values.masterHost }}
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: {{ .Values.masterHost }}-cert
