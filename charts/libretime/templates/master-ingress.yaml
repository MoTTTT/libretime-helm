apiVersion: networking.k8s.io/v1  
kind: Ingress
metadata:
  name: master
  annotations:
    cert-manager.io/cluster-issuer: lets-encrypt
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.masterHost }}
      secretName: {{ .Values.masterHost }}-cert
  rules:
    - host: {{ .Values.masterHost }}
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: liquidsoap
                port:
                  number: 8001

